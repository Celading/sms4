# Verification

Run cjpm build and cjpm test at the exact dependency commit. Regression inputs are in src/*test.cj. Use examples/basic.cj from a separate application and retain its dependency lock. Keep source, test output and package file list tied to the same revision. Test count alone is not acceptance.
